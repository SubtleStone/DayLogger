Status board:

The project now does basic text input and out to from and the sqlite database.

Fix board:

1>Text separation is unsatisfactory, work on it. Look for 3rd party solutions that self adjust.

    Implement Third party text separators for neatness.


2>Menu navigation is currently working on if else conditional statements, Switch to cases or look for other solutions

    Implement switch case or other solutions.



3>The data input is currently incomplete user can enter unrelated data anywhere because it isn't validated. (for example tags are supposed to be single words separated by spaces or some kind of separator but if user confuses it with description then it would be bad for future analytics modules, entering text instead of numerical date). 

    Implement strict data type validation before saving it.



4>The display options should feature different kinds of options, for example if someone wants borders let them have it or have it display column wise one by one at a time. Look into different ways of displaying it.


    Implement short cuts based different display methods for these tables and queries. 3rd party viewing software is also fine. Charts are a good idea, for financial spending and so on, pipeline the data from db to the charts.


5>New modules such as fitness, diet, productivity tracking to be implemented.








NEXT IN LINE:

Fix the css of the finance dashboard, implement the table below the navbar and balance.
Implement other functionalities such as add, edit, remove using in page pop ups
Find out why the ghost event fires in finance module while accessing edit, remove but the same doesnt on the main menu page, even though the match case code is the same.